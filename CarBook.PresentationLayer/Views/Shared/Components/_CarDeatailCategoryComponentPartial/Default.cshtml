@model List<CarCategory>

<div class="sidebar-box">
    <div class="categories">
        <h3>Kategorien</h3>
        @foreach (var item in Model)
        {
        
            <li><a href="#">@item.CategoryName <span>@item.Cars.Count</span></a></li>
        }    
    </div>
</div>